import 'package:flutter/material.dart';

class ComponentStyles {
  // Divider in theme styles
  static const standardDivider = Divider(
    height: 10,
    thickness: 2,
    color: Colors.black,
  );
  // IconButton size in Settings
  static const double standardIconButtonSize = 30;
}
